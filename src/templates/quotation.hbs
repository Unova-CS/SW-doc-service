<!DOCTYPE html>
<html>
<head>
  <style>
    body { font-family: Arial, sans-serif; margin: 0; padding: 30px; background: #fff; }
    .header { display: flex; justify-content: space-between; align-items: center; border-bottom: 2px solid #F59A23; padding-bottom: 15px; margin-bottom: 20px; }
    .logo { width: 120px; }
    .quote-title { font-size: 24px; color: #000; font-weight: bold; text-align: center; margin: 20px 0; }
    .quote-number { font-size: 14px; text-align: right; margin-bottom: 20px; }
    .client-info { margin: 20px 0; }
    .table { width: 100%; border-collapse: collapse; margin: 20px 0; }
    .table th, .table td { padding: 10px; text-align: left; border: 1px solid #ddd; }
    .table th { background: #F59A23; color: white; font-weight: bold; }
    .totals { margin: 20px 0; text-align: right; }
    .acceptance { margin-top: 30px; padding: 15px; background: #f9f9f9; border-left: 3px solid #F59A23; font-size: 12px; }
    .footer { font-size: 10px; text-align: center; margin-top: 40px; padding-top: 10px; border-top: 1px solid #ddd; color: #666; }
  </style>
</head>
<body>

  <!-- Header -->
  <div class="header">
    <img src="{{logoPath}}" class="logo" />
    <div style="text-align: right;">
      <strong>Smart-Ways Solutions</strong><br>
      {{company.email}} | {{company.phone}}<br>
      {{company.address}}
    </div>
  </div>

  <!-- Title -->
  <div class="quote-title">QUOTATION</div>

  <!-- Quote Number & Dates -->
  <div class="quote-number">
    <strong>Quotation No:</strong> {{number}}<br>
    <strong>Date:</strong> {{data.date}}<br>
    <strong>Valid Until:</strong> {{data.valid_date}}
  </div>

  <!-- Client Info -->
  <div class="client-info">
    <strong>Prepared For:</strong><br>
    {{data.client_company_name}}<br>
    {{data.client_company_address}}<br>
    SIRET: {{data.client_siret}}<br>
    VAT: {{data.client_vat_number}}
  </div>

  <!-- Items Table -->
  <table class="table">
    <thead>
      <tr>
        <th>Description</th>
        <th>Amount (ZMW)</th>
      </tr>
    </thead>
    <tbody>
      {{#each data.items}}
      <tr>
        <td>{{this.description}}</td>
        <td>ZMW {{this.price}}</td>
      </tr>
      {{/each}}
    </tbody>
    <tfoot>
      <tr>
        <td><strong>Total:</strong></td>
        <td>ZMW {{data.total_price}}</td>
      </tr>
    </tfoot>
  </table>

  <!-- Acceptance Section -->
  <div class="acceptance">
    <strong>Acceptance:</strong> Please sign and return a copy to confirm acceptance of this quotation.
  </div>

  <!-- Footer -->
  <div class="footer">
    Smart-Ways Solutions — All rights reserved<br>
    Plot 9812/15a, Godfrey Chitalu Road, Nyumba Yanga, Lusaka<br>
    Email: smartwayssolutions1@gmail.com | Phone: +260 777 644 161<br>
    PACRA Registration No. 320200015637
  </div>

</body>
</html>